# Synthetic Fixtures

Only small synthetic or deliberately sanitized data belongs here. `monitoring_gap.csv`
is generated example data for reader, type-inference, missing-marker, interval, and
preview tests. It contains no operational measurements.
